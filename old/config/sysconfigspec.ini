[Settings]
    name=string(default="Elixys")
    devicespecpath = string(default="config/device/devicespec.ini")

[Devices]
    [[__many__]] #Device
        version=float(default=0)
        type=string(default="Default")
        name=string(default="Default")
        configpath=string(default="config/device/Default.ini")
    
[Logging]
    configpath=string(default="config/logconfig.ini")